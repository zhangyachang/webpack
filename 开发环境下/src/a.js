export const add = (x, y) => {
  return x + y;
};

export const reduce = (x, y) => {
  x - b;
  return x - y;
};

export function print(params) {
  console.log('代码执行111');

  console.log('更新了');

  // 
}
